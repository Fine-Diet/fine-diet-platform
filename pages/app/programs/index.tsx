'use client';

/**
 * Programs Home — presentation-ready first pass
 * (FD-PLATFORM:programs-home-presentation-v1).
 *
 * Carousel-capable Baseline/recommendation hero, seeded Featured Programs,
 * and extensible Programs by Category. Live Baseline runtime attaches where safe.
 */

import { ProgramsHomeView } from '@/components/programs/home/ProgramsHomeView';

export default function ProgramsHomePage() {
  return <ProgramsHomeView />;
}
