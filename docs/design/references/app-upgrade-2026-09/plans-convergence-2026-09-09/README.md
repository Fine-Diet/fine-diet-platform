# Plans Home + Plans Day convergence references

Added September 9, 2026, to record the clarified shared meal-authoring direction across Plans Home and Plans Day. The screenshots are unmodified copies of the supplied exports; their SHA-256 provenance is in the parent manifest.

`plans-home-week-accordion-final-desktop.png` is the current visual authority for the final compact Plans Home expanded-slot presentation. It governs compact Search/Scan/`+`, Meal and Single Item rows, qty/unit controls, per-row overflow, slot Total/NDS/kcal, conditional dirty Draft + Save, the drawer hierarchy, and the Manage-local planning-state rail.

The drawer hierarchy is **Home; Manage; Day; Week; Month; Library**. The local rail is **Manage | Day | Week | Month**. Do not reinterpret the rail as Home, Manage with nested child links, and Library.

Saved Meal rows show Meal-level aggregate nutrition. Single Item rows show item-level nutrition. Slot Total uses canonical computed NDS and kcal values; do not hardcode the prototype samples. Draft + Save appear only while the slot composition is dirty.

The four PRIMARY images collectively govern inline accordion expansion, in-slot search/scan/`+` actions, compact Plans Home density, selected items with quantity/unit/remove, and conditional in-slot Draft/Save after an item is added or edited. Planned count is per occupied slot / planned meal container. Confirm before remove/delete executes.

`plans-home-week-accordion-selected-items-superseded-desktop.png` is retained only as an audit artifact. Its `Planned 2 of 4` summary is inconsistent with the three occupied slots shown and is not active authority. Use the PRIMARY references and the approved behavior recorded in `../REFERENCE-MANIFEST.md`.

Historical Plans Home/Day files in `../20-plans/` remain intact, but the convergence set supersedes them for the shared meal-authoring details named above. Earlier Plans Home convergence images also remain intact; the final image supersedes them for populated-row details, navigation treatment, nutrition scope, overflow, and dirty-state actions. They still govern their specifically documented empty and alternate-action states.
